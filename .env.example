# 後端代理模式用（選用）。複製成 .env 後填入，.env 已被 .gitignore 擋住。
# 只要設定其中一個，該供應商就會走伺服器代理，瀏覽器完全接觸不到 Key。

# DEEPSEEK_API_KEY=
# GEMINI_API_KEY=

# 允許使用代理的識別碼雜湊（逗號分隔的 sha256；留空則用內建名單）
# STUNT_ALLOWED=

# 伺服器位址（預設 127.0.0.1:8790）
# HOST=127.0.0.1
# PORT=8790
